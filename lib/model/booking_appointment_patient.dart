import 'package:freezed_annotation/freezed_annotation.dart';

part 'booking_appointment_patient.freezed.dart';
part 'booking_appointment_patient.g.dart';

@freezed
class BookingAppointmentPatient with _$BookingAppointmentPatient {
  const factory BookingAppointmentPatient({
  @Default(null) dynamic DeptName,
  @Default(null) dynamic Doctor_Notes,
  @Default(null) dynamic Hospital_id,
  @Default(null) dynamic Timing,
  @Default(null) dynamic Hospital_PhoneNumber,
  @Default(null) dynamic Allergie_id,
  @Default(null) dynamic finding,
  @Default(null) dynamic CountList,
  @Default(null) dynamic Allergie_Name,
  @Default(null) dynamic Doctor_id,
  @Default(null) dynamic test_id,
  @Default(null) dynamic test_cost,
  @Default(null) dynamic Doctor_Name,
  @Default(null) dynamic alter_flag,
  @Default(null) dynamic Doctor_Image,
  @Default(null) dynamic Doctor_Qualification,
  @Default(null) dynamic SubCategory_Name,
  @Default(null) dynamic Type_Flag,
  @Default(null) dynamic file_name,
  @Default(null) dynamic test_name,
  @Default(null) dynamic User_Name,
  @Default(null) dynamic User_Image,
  @Default(null) dynamic User_address,
  @Default(null) dynamic User_bloodgroup,
  @Default(null) dynamic User_dob,
  @Default(null) dynamic Emailid,
  @Default(null) dynamic MobileNumber,
  @Default(null) dynamic Location_Name,
  @Default(null) dynamic Hospital_Name,
  @Default(null) dynamic Hospital_Address,
  @Default(null) dynamic Clinic_id,
  @Default(null) dynamic Clinic_Name,
  @Default(null) dynamic Clinic_Address,
  @Default(null) dynamic cancel_flag,
  @Default(null) dynamic SubCategory_id,
  @Default(null) dynamic Location_id,
  @Default(null) dynamic ClinicDoctorFee,
  @Default(null) dynamic FromTime,
  @Default(null) dynamic startSplit,
  @Default(null) dynamic EndSplit,
  @Default(null) dynamic ToTime,
  @Default(null) dynamic Day,
  @Default(null) dynamic AboutClinic,
  @Default(null) dynamic Clinic_Images,
  @Default(null) dynamic Appointment_Date,
  @Default(null) dynamic Appointment_id,
  @Default(null) dynamic Appointment_Time,
  @Default(null) dynamic Confirm_Status,
  @Default(null) dynamic hospital_images,
  @Default(null) dynamic User_id,
  @Default(null) dynamic description,
  @Default(null) dynamic file,
  @Default(null) dynamic folder_date,
  @Default(null) dynamic disease,
  @Default(null) dynamic allergies,
  @Default(null) dynamic symptoms1,
  @Default(null) dynamic symptoms2,
  @Default(null) dynamic symptoms3,
  @Default(null) dynamic height,
  @Default(null) dynamic weight,
  @Default(null) dynamic temp,
  @Default(null) dynamic diabetes,
  @Default(null) dynamic date,
  @Default(null) dynamic symptoms,
  @Default(null) dynamic view_status,
  @Default(null) dynamic files,
  @Default(null) dynamic prescription,
  @Default(null) dynamic lab,
  @Default(null) dynamic pat_list,
  @Default(null) dynamic summary,
  @Default(null) dynamic hos_list,
  @Default(null) dynamic total,
  @Default(null) dynamic waiting,
  @Default(null) dynamic upcoming,
  @Default(null) dynamic completed,
  @Default(null) dynamic dosage,
  @Default(null) dynamic drug_name,
  @Default(null) dynamic frequency,
  @Default(null) dynamic duration,
  @Default(null) dynamic instruction,
  @Default(null) dynamic notes,
  @Default(null) dynamic error,
  @Default(null) dynamic treatment_date,
  @Default(null) dynamic treatment_id,
  @Default(null) dynamic treatment_name,
  @Default(null) dynamic cost,
  @Default(null) dynamic days,
  @Default(null) dynamic discount,
  @Default(null) dynamic grant_total,
  @Default(null) dynamic treatment,
  @Default(null) dynamic bill_date,
  @Default(null) dynamic bill_id,
  @Default(null) dynamic bill_name,
  @Default(null) dynamic file_type,
  @Default(null) dynamic upload_date,
  @Default(null) dynamic billing,
  @Default(null) dynamic labtest_date,
  @Default(null) dynamic labtest_id,
  @Default(null) dynamic test_code,
  @Default(null) dynamic labtest_name,
  @Default(null) dynamic labtest,
  @Default(null) dynamic labname,
  @Default(null) dynamic app_amount,
  @Default(null) dynamic eclinic_amount,
  @Default(null) dynamic service_amount,
  @Default(null) dynamic app_count,
  @Default(null) dynamic eclinic_count,
  @Default(null) dynamic service_count,
  @Default(null) dynamic lab_id,
  @Default(null) dynamic lab_name,
  @Default(null) dynamic jan,
  @Default(null) dynamic feb,
  @Default(null) dynamic mar,
  @Default(null) dynamic apr,
  @Default(null) dynamic may,
  @Default(null) dynamic jun,
  @Default(null) dynamic jul,
  @Default(null) dynamic aug,
  @Default(null) dynamic sep,
  @Default(null) dynamic oct,
  @Default(null) dynamic nov,
  @Default(null) dynamic dec,
  @Default(null) dynamic specialization,
  @Default(null) dynamic Lab_Result,
  @Default(null) dynamic Payment_Flag,
  @Default(null) dynamic E_Flag,
  @Default(null) dynamic Test_Status_Flag,
  @Default(null) dynamic Follow_Up_Date,
  @Default(null) dynamic Follow_Up_Time,
  @Default(null) dynamic Follow_Up_Flag,
  @Default(null) dynamic Sample_Flag,
  @Default(null) dynamic Sample_Date,
  @Default(null) dynamic Result_Flag,
  @Default(null) dynamic Result_Date,
  @Default(null) dynamic Result_Time,
  @Default(null) dynamic Expected_Date,
  @Default(null) dynamic cancelalert_hour,
  @Default(null) dynamic cancelalert_notes,
  @Default(null) dynamic NightTime,
  @Default(null) dynamic AfternoonTime,
  @Default(null) dynamic MorningTime,
  @Default(null) dynamic FoodInstr,
  @Default(null) dynamic PresHalf_id,
  @Default(null) dynamic Test_Result_Property,
  @Default(null) dynamic Approved_Status,
  @Default(null) dynamic ReleasedStatus,
  @Default(null) dynamic Intake_Object,
  @Default(null) dynamic Key_Id,
  @Default(null) dynamic order_id,
  @Default(null) dynamic Confirm_Mail_Flag,
  @Default(null) dynamic Vital_Flag,
  @Default(null) dynamic Token_Number,
  @Default(null) dynamic R_App,
  @Default(null) dynamic C_App,
  @Default(null) dynamic OT_RequestCount,
  @Default(null) dynamic IP_app_count,
  @Default(null) dynamic Emergrncy_app_count,
  @Default(null) dynamic Missed,
  @Default(null) dynamic confirmed,
  @Default(null) dynamic IP_waiting,
  @Default(null) dynamic OT_Completed,
    //"DiseaseDetails_Flag": "0",
    //         "Prescription_Flag": "0",
    //         "LabTest_Flag": "0"
  @Default(null) dynamic DiseaseDetails_Flag,
  @Default(null) dynamic Prescription_Flag,
  @Default(null) dynamic LabTest_Flag,
  @Default(null) dynamic MediaAvailablity,
    @Default(null) dynamic complaints,
  }) = _BookingAppointmentPatient;
  static const fromJsonFactory = _$BookingAppointmentPatientFromJson;
  factory BookingAppointmentPatient.fromJson(Map<String, dynamic> json) =>
      _$BookingAppointmentPatientFromJson(json);
}

